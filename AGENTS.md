# Repository workflow preference

For this repository, work directly on `master` by default. Do not create task branches, switch to feature branches, merge branches, or commit changes unless the user explicitly requests that action. Leave requested changes in the working tree so the user can inspect and stage them manually.
